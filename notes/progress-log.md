# Progress Log

> Detailed, dated log of every completed topic. `ai-curriculum.md` stays index-only (checkboxes); this file holds the narrative history.


- **2026-06-19**: Curriculum created. Starting point confirmed zero across math and ML.
- **2026-06-19**: Cross-verified against current (June 2026) research. Added frontier topics, tooling reference.
- **2026-06-19**: Clarified "from scratch" philosophy. Phase 3 includes own autograd engine.
- **2026-06-19**: Full merge with rohitg00/ai-engineering-from-scratch (503 lessons, 20 phases). Repo cross-references added per topic throughout. All missing topics incorporated: information theory, Fourier, graph theory, SVMs, KNN, anomaly detection, time series, Word2Vec, RNNs, GANs, VAEs, voice cloning, MCP deep dive (23 lessons), agent engineering (42 lessons), autonomous systems, multi-agent swarms, ethics/alignment, capstone projects.
- **2026-07-04**: Phase 0 complete — all 8 setup lessons done. Environment solid: Python 3.14.3, venv, numpy 2.5.0, matplotlib 3.11.0, jupyter, VS Code configured, RTX 2060 GPU confirmed (CUDA 13.2), Docker and data management understood conceptually, debugging/profiling tools covered. Starting Phase 1 (Math Foundations) next session.
- **2026-07-04**: 1.1.1 done — variables, expressions, solving linear equations for x. Learner solved 3/3 practice problems correctly, verified conceptually.
- **2026-07-04**: 1.1.2 done — functions, f(x) notation, domain/range. Coded g(x)=1/x and h(x)=x²-4 in Python (`phase1-math/1_1_functions.py`), correctly predicted ZeroDivisionError for g(0) before running, correctly identified both roots of h(x). First encounter with reading a Python traceback.
- **2026-07-04**: 1.1.3 done — slope/intercept, y=mx+b. Coded `line()` and `slope()` in `phase1-math/1_3_slope.py`, generated table for m=2,b=3, verified slope=2 using non-adjacent points (extra rigor beyond what was asked), tested negative slope (m=-5,b=5) and confirmed decreasing y. Correctly predicted m=0 gives a horizontal line at y=b.
- **2026-07-04**: 1.1.4 done — exponents and logarithms. Coded `phase1-math/1_1_4_logs.py`, verified log2(16)=4, log10(100)=2, log(1)=0, and confirmed 2**10=1024 inverts to log2(1024)=10.0. Correctly predicted math.log(0) would raise an error; ran it and got ValueError: expected a positive input (Python 3.14's updated wording for the classic "math domain error"). Correctly reasoned that log(0) is undefined because a positive base raised to any real exponent is always >0, never reaching exactly 0. **Section 1.1 (Algebra) fully complete** — moving to 1.2 (Calculus) next session. Also corrected: repo cross-references for all of 1.1 were wrong (see notes) — no confirmed matching lesson found yet for basic algebra topics in rohitg00/ai-engineering-from-scratch; will check catalog.html as needed rather than guess.
- **2026-07-04**: 1.2.1 done — derivatives as local slope, numeric intuition. Coded `numeric_derivative()` in `phase1-math/2_1_derivative.py`, computed derivative of x² at x=3,0,-2, discovered the 2x pattern from the numbers directly. Explored h shrinking (0.0001 → more accurate at 1e-8) and was introduced to catastrophic cancellation as the reason accuracy degrades if h shrinks too far (ties to 1.3.12 numerical stability, upcoming). Correctly reasoned that a line's derivative equals its constant slope (2 for f(x)=2x+3, 0 for f(x)=5) by connecting back to 1.1.3's slope concept, after an initial miss (confused "derivative" with "solve for x").
- **2026-07-04**: 1.2.2 done — power rule and chain rule, worked by hand and verified numerically. Coded `phase1-math/1_2_2_rules.py`; generalized power rule to any n (not just the x³ example given), verified against numeric_derivative for x³ at x=3 (27 vs 27.0009) and chain rule on (2x+3)² at x=3 (36 vs 36.0004). Correctly predicted derivative of x¹ is 1 by applying the rule mechanically, connected to 1.1.3 (slope of f(x)=x is 1).
- **2026-07-04**: 1.2.3 done — partial derivatives, multi-variable functions. Coded `phase1-math/1_2_3_partials.py`, generalized f(x,y,n)=xⁿ+yⁿ with parameterized n, verified ∂f/∂x=2x, ∂f/∂y=2y at (3,4) numerically (6.0001, 8.0001). Tested f(x,y)=x·y, correctly discovered ∂f/∂x=y, ∂f/∂y=x (the two variables swap — seed of the product rule, not formally named in curriculum but self-discovered). Extended to three variables x²+y²+z² at (3,4,5), all three partials correct (6.0001, 8.0001, 10.0001), correctly reasoning each partial only depends on its own variable.
- **2026-07-04**: 1.2.4 done — gradients and gradient descent. Coded `phase1-math/1_2_4_gradient.py` (`gradient()`, `gradient_step()`), verified ∇f(3,4)=[6,8] for f(x,y)=x²+y². Ran 50-step gradient descent loop (first exposure to Python `for`/`range`/modulo, translated from JS) starting at (3,4): watched x,y monotonically shrink toward 0 and f(x,y) fall from 25→4.8, correctly observing the step size naturally decelerating near the minimum (gradient shrinks as x,y shrink). **This closes the numeric-calculus arc (1.2.1-1.2.4) that together constitute the full mechanism of gradient descent** — next lessons (1.2.5 optimization, 1.2.6 convexity) formalize what was just observed empirically.
- **2026-07-04**: 1.2.5 done — optimization, local vs global minima, why we move opposite the gradient. Coded `phase1-math/2_5_optimization.py` using f(x)=x⁴-4x² (two symmetric minima at ±√2≈±1.41421). Self-caught and fixed a real indentation bug (return statement nested inside the for loop, causing early exit after 1 step). Final correct 100-step results: x_start=1.5→1.41416, x_start=-1.5→-1.41426 (both fully converged to the two separate valleys). x_start=0.01→1.4127 (nearly fully converged — escaped the near-zero-gradient plateau near the local max at x=0 after initial slow start). x_start=0 exactly→0.1096 (floating-point imprecision in the numeric derivative broke the theoretical zero-slope unstable equilibrium at the hilltop; the tiny initial nudge compounded over 100 real steps into visible escape/movement — first exposure to unstable equilibria and plateaus as real training phenomena). Requested a mid-session detour first to explain conceptually *why* slope→derivative→partial derivative→gradient are all needed rather than accepted as arbitrary rules — addressed as one continuous idea (single input, then curved, then multiple inputs, then all inputs at once) before continuing.
- **2026-07-04**: 1.2.6 done — convexity, conceptual close to section 1.2, no new code. Correctly identified straight lines as the convex boundary case (connecting segment lies exactly on the curve). Initial answer on f(x)=-x² needed one correction: gradient descent minimizing it doesn't settle at "endpoints" but diverges toward -∞ in whichever direction it starts drifting, since the function is unbounded below — correctly proposed gradient ascent as the fix. Correctly concluded the learner's own Jarvis-platform models will have non-convex loss landscapes; one phrasing corrected — gradients are always locally accurate, non-convexity is a visibility problem (no knowledge of better valleys elsewhere), not a correctness problem. **Section 1.2 (Calculus) fully complete** — derivative, rules, partial derivatives, gradients, optimization, and convexity now form one continuous, hands-on-verified arc. Moving to 1.3 (Linear Algebra) next session — this is also where the previously-reviewed real repo lesson (`01-linear-algebra-intuition`, vectors/matrices/dot products/rank) becomes a genuinely relevant second-pass reference.
- **2026-07-04**: 1.3.1 done — vectors, addition, scaling, geometric intuition. Coded `phase1-math/1_3_1_vectors.py` (`vector_add`, `vector_scale`, first use of Python list comprehensions, framed against JS `.map()`). Verified [3,2]+[1,4]=[4,6], 3×[3,2]=[9,6], -1×[3,2]=[-3,-2] (direction flip), 0×[3,2]=[0,0] (zero vector, no direction). Computed first linear combination by hand and in code: 2×[1,1]+3×[2,0]=[8,2] — explicitly connected to what a single neuron computes (weighted sum of inputs), first direct bridge from linear algebra to a neural network operation.
- **2026-07-04**: 1.3.2 done — dot product, arithmetic and geometric meaning. Coded `phase1-math/1_3_2_dot_product.py` (first use of a generator expression, `sum(... for ...)`, framed against JS `.reduce()`). Verified [3,4]·[1,2]=11, [3,4]·[-3,-4]=-25 (opposite directions → strongly negative), [3,4]·[4,-3]=0 (perpendicular vectors → exactly zero). Own example with roughly-similar-direction 3D vectors gave 837.5 (strongly positive), confirming the angle intuition. Correctly identified dot product's key distinguishing feature (scalar output vs. linear combination's vector output); refined with the connection that a dot product is structurally a linear combination where the scaling weights come from the other vector itself, collapsing to one number.
- **2026-07-04**: 1.3.3 done — matrices, shape, notation. Coded `phase1-math/1_3_3_matrices.py` (`matrix_shape`, `get_row`, `get_column`), verified W (2×3) has shape (2,3), get_row(W,0)=[0.1,0.2,0.3], get_column tested at index 1 correctly returning [0.2,0.5] (0-indexed second column) — function logic confirmed correct for any column index. Correctly predicted transposing a 2×3 matrix yields 3×2. Note: tutor introduced an internal inconsistency mixing 1-indexed math column language in the worked example with a 0-indexed verification target in the exercise instructions — flagged and corrected in-session; not a learner error.
- **2026-07-04**: 1.3.4 done — matrix-vector multiplication. Coded `phase1-math/1_3_4_matrix_vector.py`, reimplemented dot_product/get_row inline (first exposure to Python imports, explained conceptually but not yet used). Verified M·v=[11,20] for M=[[2,1],[0,4]], v=[3,5]. Applied to the ML-relevant case: W (2×3, two neurons)·x=[1,2,3] gave [1.4, 3.1999999999999997], correctly computed by hand first, floating-point tail explained as the same representation quirk from earlier lessons. Strong independent reasoning on the shape-mismatch prediction: correctly distinguished two distinct failure modes based on implementation detail (which vector's length drives the dot-product loop) — M=2×2,v=length3 silently ignores v's extra element (no error); M=4×4,v=length3 throws IndexError. Both predicted correctly before running, with accurate reasoning about *why* each occurs, not just the outcome.
- **2026-07-04**: 1.3.5 done — matrix-matrix multiplication, why shapes must match. Coded `phase1-math/1_3_5_matrix_matrix.py`. Verified A·B=[[19,22],[43,50]] for two 2×2 matrices. Applied to batch case: W(2×3, two neurons)·X(3×2, batch of 2 inputs)=[[1.4,3.2],[3.2,7.7]] — first full mini-batch forward pass computed by hand and in code. Proactively added input-shape validation before being asked (own initiative) — independently reinvented the defensive-programming pattern real frameworks (NumPy/PyTorch) use for shape-mismatch errors. Confirmed predicted IndexError when validation removed and shapes genuinely mismatched (2×3 × 2×3), correctly tracing the cause to dot_product's loop bound coming from the row length exceeding the column length available.
- **2026-07-04**: 1.3.6 done — matrix transformations and eigenvalues. Extensive line-by-line walkthrough requested and given for `check_eigenvector`'s ratio/tolerance logic (list comprehension with filter condition, generator expression inside `all()`, why `abs(diff) < 1e-9` rather than `diff > 0` or rounding to whole numbers) — good clarifying questions throughout, each addressed before continuing to code. Coded `phase1-math/1_3_6_eigen.py`. Verified M=[[2,0],[0,1]]: [1,0] is an eigenvector with λ=2, [1,1] is not an eigenvector (different components scaled differently). Verified N=[[0,1],[1,0]] (reflection across y=x): [1,1] is an eigenvector with λ=1 (lies on the mirror line, unchanged), [1,-1] is an eigenvector with λ=-1 (perpendicular to the mirror line, flips direction exactly) — correctly predicted both before running. Discovered independently-verifiable fact: a reflection matrix has exactly two eigenvector directions (the mirror line and its perpendicular), everything else gets rotated.
- **2026-07-04**: 1.3.7 done — norms and distances, L1, L2, Euclidean distance, cosine similarity. Coded `phase1-math/1_3_7_norms.py`. Verified L1([3,4])=7, L2([3,4])=5.0, euclidean_distance([3,4],[1,2])≈2.828 (matches hand-computed √8), cosine_similarity([3,4],[1,2])≈0.984 (matches 1.3.2's dot-product-based worked example). Key result: cosine_similarity([3,4],[6,8])=1.0 — confirmed cosine similarity is completely length-invariant, only measures direction, directly explaining why it's the standard tool for embedding comparison in semantic search (Phase 8.2) over raw dot product, which conflates angle and magnitude.
- **2026-07-04**: 1.3.8 done — tensors, shape, axes, broadcasting. Requested and received a full line-by-line recursion trace for `get_shape` (tuple concatenation, base case via isinstance, unwinding through 4 nested calls on a 2×2×2 example) after an initial "pushed to result" misdescription was corrected to "each call waits, then builds a bigger tuple on the way back up." Coded `phase1-math/1_3_8_tensors.py`. Two real bugs self-produced and fixed during the session: (1) a copy-paste slip testing `v` twice instead of testing `M`, caught and corrected; (2) `broadcast_add_bias` initially used a flat (non-nested) list comprehension with two chained `for` clauses, producing `[11,14,22,25,33,36]` instead of a proper nested matrix — fixed to the correct nested-comprehension pattern (matching 1.3.5's matrix-multiply structure), producing `[[11,22,33],[14,25,36]]`. Verified get_shape on vector (3,), matrix (3,2), and 3D nested list (2,2,2) — all correct. Confirmed the generalized shape-mismatch pattern (same root cause as 1.3.4/1.3.5) on two new cases: b shorter than M's columns → silent incomplete result; b longer → IndexError. **Section 1.3 progress: 8 of 12 lessons complete.**
- **2026-07-04**: 1.3.9 done — SVD, concept and NumPy tool usage (not hand-derived — first lesson using NumPy for real computation rather than only from-scratch loops, per curriculum's "NumPy is allowed" rule). Requested a full plain-language re-explanation before starting (concept was initially difficult to grasp) — rebuilt from "SVD ranks a matrix's content by importance, U/Vt rotate, Σ holds the ranked stretch amounts" framing, plus a complete line-by-line code walkthrough (np.array, np.linalg.svd unpacking three return values via tuple-unpacking, @ as matrix multiplication, np.diag converting a list to a diagonal matrix) before any code was run. Coded `phase1-math/1_3_9_svd.py`. Verified M=[[3,0],[0,1]] gives singular values [3,1] with identity U/Vt (already axis-aligned, no rotation needed). Verified M2=[[4,0],[3,-5]] gives singular values [6.325,3.162], reconstruction matched M2 within floating-point noise. Compression demo: zeroing the smaller singular value gave a rough, notably inaccurate approximation ([[2,-2],[4,-4]] vs real [[4,0],[3,-5]]) — correctly predicted it wouldn't be exact, though initial reasoning conflated "which values get dropped" with "already-zero values," corrected to the real explanation: compression only works well when there's a large gap between important and unimportant singular values; this toy example's two values were too close in magnitude (6.3 vs 3.2) for the approximation to be good, unlike real large matrices where the value gap is typically much larger.
- **2026-07-04**: Two substantial follow-up addenda added to 1.3.9's notes post-close: (1) full hand-derivation of where singular values actually come from (√eigenvalues of MᵀM, verified numerically on M2, explained why M1 looked deceptively simple/diagonal); (2) precise definition of U/V as orthonormal eigenvector bases of MMᵀ/MᵀM, verified directly against M2's real output (unit length, perpendicularity, eigenvector identity all checked by hand), plus the sign-ambiguity subtlety.
- **2026-07-04**: 1.3.10 done — PCA from scratch (via NumPy tools) and t-SNE/UMAP concept. Requested and received a full line-by-line code walkthrough before running (mean/axis=0, broadcasting via `data-mean`, covariance matrix as the MᵀM pattern from 1.3.9's addendum divided by n-1, np.linalg.eig as the "find eigenvectors" counterpart to 1.3.6's "check eigenvector," np.argsort + [::-1] for descending sort, 2D column-indexing `[:, order[:n]]`). Coded `phase1-math/1_3_10_pca.py` on a near-linear 4-point dataset. Correctly predicted and confirmed a massive eigenvalue gap (3.333 vs ~0), correctly identified the dominant eigenvector [0.707,0.707] matching the data's true [1,1] line direction, and confirmed the reduced 1D values were evenly spaced (by √2, matching the [1,1] step length) — all predictions verified. New gotcha encountered and explained: `np.linalg.eig` returns complex-typed results (`+0.j`) even for guaranteed-real eigenvalues, since NumPy can't assume realness for general matrices in advance. Conceptual question (PCA vs t-SNE/UMAP) needed correction: initial framing ("straightforward task" vs "multiple truths") was vague; refined to the real distinction — PCA preserves global/linear structure (good for compression, interpretable axes), t-SNE/UMAP preserve local neighborhood relationships even at the cost of distorting global shape (good for visualizing non-linear clusters, e.g. semantically similar code snippets in an embedding space). **Section 1.3 progress: 10 of 12 lessons complete** — only linear systems (1.3.11) and numerical stability (1.3.12) remain.
- **2026-07-04**: Brief follow-up on t-SNE/UMAP depth (1.3.10) — confirmed the shallow treatment was intentional per the curriculum's own wording ("PCA from scratch" vs "t-SNE/UMAP concept"), and added extra intuition: t-SNE minimizes a KL-divergence-based loss between high-D and low-D neighbor-probability distributions via gradient descent (same core mechanism as 1.2.4, different loss function); UMAP is topology-based (out of scope), generally faster and better at preserving global structure, and the more commonly used default in practice today. Not added as a formal notes addendum per learner's choice to move on.
- **2026-07-04**: 1.3.11 done — linear systems, solving Ax=b. Connected explicitly back to 1.1.1 (single-equation solving, now generalized to many equations/unknowns via matrices). Coded `phase1-math/1_3_11_linear_systems.py` using `np.linalg.solve`. Verified A=[[2,1],[1,-1]],b=[11,1] → x=[4,3] (matches hand-worked example using the 2×2 inverse formula and determinant, introduced for the first time). Own example: A=[[3,2],[1,-1]],b=[12,1] → x=[2.8,1.8], verified by substitution into both original equations. Correctly predicted det(A)=0 for A=[[2,4],[1,2]] means no inverse exists; ran anyway and got `LinAlgError: Singular matrix` — reasoning ("1/0 crashes it") was directionally right but refined to the precise explanation: NumPy's solve() doesn't use naive inverse-and-multiply internally (previews 1.3.12), but hits the same underlying wall — det=0 means a **singular matrix**, geometrically because the second row was an exact scalar multiple of the first (same line written twice, not two independent constraints), connected back to 1.3.6 (singular matrices always have 0 as an eigenvalue). **Section 1.3 progress: 11 of 12 lessons complete** — only numerical stability (1.3.12) remains to close out linear algebra entirely.
- **2026-07-04**: 1.3.12 done — numerical stability. Explicit callback to every floating-point theme recurring since 1.2.1. Coded `phase1-math/1_3_12_numerical_stability.py`. Underflow demo: direct multiplication of 200 copies of 0.01 collapsed to exactly 0.0 around iteration 162 (float64's absolute floor, ~5e-324); the log-space equivalent stayed precise the entire time (-833.5 at iteration 180), concretely demonstrating why 1.1.4 flagged log as a numerical-stability necessity, not decoration. Catastrophic cancellation, sharpest version yet: predicted `(1.0+1e-16)-1.0` would print ~1e-16 (correct in principle) — actual result was exactly 0.0, a genuine miss corrected by introducing **machine epsilon** (~2.22e-16 near magnitude 1.0): 1e-16 is smaller than the smallest representable gap near 1.0, so the addition itself silently rounds away before any subtraction occurs — total information loss, not partial. Conceptual question on why NumPy's internal algorithms (LU/QR decomposition, named but not built) beat naive inverse-via-determinant needed a full rebuild from scratch (initial "not sure I understand" response) — resolved by connecting directly to the just-observed cancellation result: naive inverse computation involves many subtraction/division steps, each a potential cancellation site, especially as matrices grow or determinants shrink; NumPy's real algorithms are structured specifically to avoid ever subtracting dangerously close numbers. **Section 1.3 (Linear Algebra) — fully complete: all 12 lessons closed.** Moving to 1.4 (Probability & Statistics) next session.
- **2026-07-04**: 1.4.1 done — mean, variance, standard deviation by hand. First lesson of Phase 1.4 (Probability & Statistics). Coded `phase1-math/1_4_1_mean_variance.py`. Verified [2,4,4,4,5,5,7,9] → mean=5, variance=4, std=2, matching the worked example. Verified [3,4,5,6,7] (same mean, tighter spread) → variance=2 (correctly smaller than the first dataset, confirming the "tighter data → lower variance" intuition), std≈1.414. Correctly predicted and confirmed [5,5,5,5,5] (identical values) → variance=0, std=0, reasoning precisely that every point's distance from the mean is 0. Noted zero-variance as a real, meaningful edge case (a feature that never varies carries no learnable information for a model).
- **2026-07-04**: 1.4.2 done — probability basics: events, independence, combinations. Coded `phase1-math/1_4_2_probability.py`. Verified P(rolling a 4)=1/6≈0.1667. Verified P(heads,heads)=0.25 for two independent coin flips; cleverly reused `independent_and` recursively (rather than new code) for three flips, correctly getting 0.125 — noted this halving pattern (0.5→0.25→0.125→...) as the same underflow-toward-zero shape from 1.3.12, just friendlier starting numbers. Verified C(4,2)=6 (matched hand-listed pairs) and C(6,3)=20, correctly predicting the larger pool gives more combinations. Correctly predicted C(n,n)=1 (choosing everything has exactly one outcome regardless of n) and confirmed with C(5,5)=1.0.
- **2026-07-04**: 1.4.3 done — Bayes' theorem, medical test worked example. Coded `phase1-math/1_4_3_bayes.py`. Verified the classic 1%-prevalence/95%-accuracy test example gives P(disease|positive)≈0.161, far below the naive "95%" gut instinct — genuinely internalized why (false positives from the large healthy population outnumber true positives from the rare disease group). Verified p_a=0.5 (common disease) gives exactly 0.95, correctly explained as the prior perfectly canceling out when prevalence is 50/50 with symmetric error rates, collapsing the posterior to the test's raw accuracy — confirming "more common disease → more trustworthy positive test" using the same test. Initial worse-test experiment (#3) inadvertently changed two parameters at once (sensitivity AND false-positive rate) rather than isolating sensitivity alone; asked a strong clarifying question — "why isn't P(B|not A) just 1-P(B|A)?" — correctly resolved by distinguishing that these describe two separate populations (sick vs. healthy) with independently-measured error rates, not complementary probabilities within one population (only P(B|A)+P(not B|A)=1 holds, within a fixed condition). Reran with sensitivity isolated (0.6, false-positive rate unchanged at 0.05) → 0.108, correctly lower than 0.161 but a smaller drop than the combined-effect run (0.0149), correctly distinguishing degrading one test property vs. both.
- **2026-07-04**: 1.4.4 done — Gaussian/normal distribution, sampling via NumPy (PDF formula not hand-derived, genuinely advanced — used np.random.normal as a tool, consistent with the "NumPy allowed" pattern from 1.3.9 onward). Real early confusion caught and resolved: mixed up `np.random.seed(42)` (reproducibility) with sample count, calling `n=42` and never actually invoking `.seed()` — resulting in only 42 samples and non-reproducible results. Good follow-up question asked ("we're not passing 42 anywhere, so how does seeding connect?") — resolved by explaining NumPy's random generator as **stateful** (a hidden global cursor all `np.random.*` calls silently read from and advance), contrasted with the pure functions built throughout this curriculum so far, and compared to JS's unseedable `Math.random()`. After the fix, coded `phase1-math/1_4_4_gaussian.py` correctly (seed as its own line, n=1000). Verified mean=0,std=1 → empirical ≈(0.019, 0.979), close but not exact to true parameters (correctly expected — sampling noise, formalized later in 1.4.7). Verified mean=100,std=15 (IQ-score parameters) → min/max [55.9,147.9], roughly ±3 standard deviations from the mean, confirming values stay in a reasonable band. Verified std=1 vs std=10 (same mean=0): range 7.09 vs 69.46 — roughly 10x bigger, tracking the 10x std ratio almost exactly, confirming the predicted direction and rough proportionality.

---

- **2026-07-05**: **Restructuring session** — split docs into three files per feedback: `ai-curriculum.md` trimmed back to index-only (Progress Log section now just points here), `progress-log.md` created (this file, holding all dated narrative history), `notes/phase1-math.md` fully rewritten with explicit **Formula:** lines added to every topic (previously embedded in prose only) and all code blocks removed (redundant with committed `.py` files). No lesson content changed, only presentation/organization.
- **2026-07-05**: 1.4.5 done — expected value. Coded `phase1-math/1_4_5_expected_value.py`. Real, useful bug: initial fair-die probabilities were `[1/6,2/6,3/6,4/6,5/6,6/6]` (increasing, summing to 3.5) instead of equal `1/6` each — self-debugged with print statements, caught after being pointed to the "probabilities must sum to 1" sanity check. Corrected version gave 3.5 ✓. Verified game example ([10,2,-5] at [0.2,0.5,0.3]) → 1.5 ✓. Modified game (loss probability increased to 0.5) → -0.7, correctly predicted to flip negative. Explained in own words why weighting the worst outcome more heavily pulls expected value down (reasonable, refined to the precise mechanic: bigger weight on the negative term plus smaller weights on positive terms both push the weighted sum down simultaneously).
- **2026-07-05**: 1.4.5 done — expected value. Coded `phase1-math/1_4_5_expected_value.py`. Real, useful bug: initial fair-die probabilities were increasing (`[1/6,2/6,...,6/6]`, summing to 3.5) instead of equal 1/6 each — self-debugged with print statements, caught via the "probabilities must sum to 1" sanity check. Corrected → 3.5 ✓. Verified game example → 1.5 ✓. Modified game (loss probability raised) → -0.7, correctly predicted to flip negative. Explained in own words why weighting the worst outcome more heavily pulls expected value down.
- **2026-07-05**: 1.4.6 done — Monte Carlo sampling, estimating π. Coded `phase1-math/1_4_6_monte_carlo.py`. n=1000 and n=100000 both landed close to true π in this run (3.14, 3.14) — first run somewhat lucky for the small sample. Three repeated n=1000 runs (no fixed seed) gave genuinely different results (3.168, 3.148, 3.116), directly demonstrating sampling variance shrinking with larger n (1.4.4 theme, reinforced). Batch-size-training conceptual connection needed full building from "not sure" — successfully connected: batch size plays the same role as n_samples, larger batches give gradient estimates closer to the "true" gradient with less run-to-run variance, smaller batches are noisier (mirroring the observed pi-estimate spread). Noted as a preview (not required depth): a bit of gradient noise from smaller batches can actually help escape shallow local minima, so "bigger batch = strictly better" isn't the full picture (deferred to Phase 3).
- **2026-07-05**: 1.4.7 done — statistics for ML: bias, variance, confidence intervals, hypothesis testing. Coded `phase1-math/1_4_7_statistics.py`. Verified 95% CI for n=1000 Gaussian samples correctly contains the true mean (0). Verified the √n width-scaling prediction numerically: n=100→10000 (100x samples) gave width ratio ≈9.55 (predicted 10x); n=100→1000 (10x samples) gave ratio ≈3.10 (predicted √10≈3.16x) — both close matches, small deviation attributed to different random seeds/datasets rather than formula error. Correctly connected 1.4.6's three π estimates (3.168,3.148,3.116) to **variance** rather than bias, correctly reasoning the errors were scattered on both sides of the true value (not consistently one-directional) — contrasted with what true bias would look like (systematically wrong in one direction every time). Connected directly to the upcoming underfitting (high bias) vs overfitting (high variance) framework, Phase 2.5. **Section 1.4 (Probability & Statistics) — fully complete: all 7 lessons closed.** Moving to 1.5 (Information Theory) next session.